spring:
  application:
    name: backend
  config:
    import: "classpath:./config/local-var.yml"
  rabbitmq:
    host: localhost
    port: 5672
    username: ${rabbitmq.username}
    password: ${rabbitmq.password}

server:
  port: 7070